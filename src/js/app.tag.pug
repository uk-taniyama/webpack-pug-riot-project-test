app-tag
  panel-tag(outer="{this}")
    yield(to="header")
      span HEADER
    span CONTENT
    button(onclick="{opts.outer.onclick}") BUTTON
  script.
    console.log("start app-tag.")
    onclick = (e) => {
      console.log("ONCLICK");
    }

panel-tag
  div(class="header")
    yield(from="header")
  div(class="content")
    yield
  script.
    console.log("start panel-tag.");
    this.opts = opts;
